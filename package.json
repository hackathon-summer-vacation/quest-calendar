{
  "name": "quest-calendar",
  "version": "1.0.0",
  "description": "クエスト形式のタスク管理アプリ",
  "scripts": {
    "install:frontend": "cd questCalendar && npm install",
    "install:all": "npm run install:frontend",
    "start": "cd questCalendar && npx expo start --web",
    "start:frontend": "cd questCalendar && npx expo start --web",
    "build": "cd questCalendar && npx expo export -p web",
    "build:github-pages": "cd questCalendar && npx expo export -p web --output-dir dist",
    "build:pages-branch": "cd questCalendar && npx expo export -p web --output-dir docs && node ../scripts/copy-pages-build.mjs"
  },
  "workspaces": [
    "questCalendar"
  ]
}
