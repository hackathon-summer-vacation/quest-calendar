version: 1
frontend:
  phases:
    preBuild:
      commands:
        - cd questCalendar
        - npm ci
    build:
      commands:
        - npx expo export
  artifacts:
    baseDirectory: questCalendar/dist
    files:
      - '**/*'
  cache:
    paths:
      - questCalendar/node_modules/**/*