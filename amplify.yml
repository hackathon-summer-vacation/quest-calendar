version: 1
backend:
  phases:
    build:
      commands:
        - amplifyPush --simple
frontend:
  phases:
    preBuild:
      commands:
        - nvm install 20
        - nvm use 20
        - npm ci
        - npx ampx generate outputs --app-id d3jqkwfiqsbcui --branch foramp --stack amplify-questcalendar-tmkttmkt-sandbox-299e-amplifyDataTodoNestedStackTodoNestedStackR-1RIVZBO686C0L


    build:
      commands:
        - npx expo export
  artifacts:
    baseDirectory: dist
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*